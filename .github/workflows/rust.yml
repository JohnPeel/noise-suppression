name: Rust

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  ci:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        rust:
          - stable
          # - beta
          # - nightly
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest
        arch:
          - x86_64
          - i686
        package:
          - lv2
          - ladspa
          - mono-vst2
          - stereo-vst2
        exclude:
          - os: macos-latest
            package: lv2
          - os: macos-latest
            arch: i686
        include:
          - os: ubuntu-latest
            os_name: linux
          - os: windows-latest
            os_name: windows
          - os: macos-latest
            os_name: macos
          - package: lv2
            name: lv2
          - package: ladspa
            name: ladspa
          - package: mono-vst2
            name: mono_vst2
          - package: stereo-vst2
            name: stereo_vst2
          - os: ubuntu-latest
            ext: so
            libprefix: lib
            strip: true
            strip_args: --strip-all
          - os: windows-latest
            ext: dll
            libprefix:
            strip: true
            strip_args: --strip-all
          - os: macos-latest
            ext: dylib
            libprefix: lib
            strip: false
            strip_args:
    steps:
      - uses: actions/checkout@v2
      - uses: actions/cache@v2
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
            !target/release/${{ matrix.libprefix }}noise_suppression_*.${{ matrix.ext }}
          key: ${{ runner.os }}-${{ matrix.rust }}-${{ matrix.arch }}
      - name: Remove old Binaries
        run: rm -f target/release/${{ matrix.libprefix }}noise_suppression_*.${{ matrix.ext }}
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: ${{ matrix.rust }}-${{ matrix.arch }}
          override: true
          components: clippy
      - uses: actions-rs/cargo@v1
        with:
          command: clippy
          args: --package noise-suppression-${{ matrix.package }} -- -D warnings
      - uses: actions-rs/cargo@v1
        with:
          command: test
          args: --package noise-suppression-${{ matrix.package }}
      - uses: actions-rs/cargo@v1
        with:
          command: build
          args: --package noise-suppression-${{ matrix.package }} --release
      - name: Strip Binaries
        run: strip ${{ matrix.strip_args }} target/release/${{ matrix.libprefix }}noise_suppression_*.${{ matrix.ext }}
        if: ${{ matrix.strip }}
      - name: Create Distribution
        run: |
          mkdir dist/
          cp "target/release/${{ matrix.libprefix }}noise_suppression_${{ matrix.name }}.${{ matrix.ext }}" dist/noise_suppression_${{ matrix.name }}.${{ matrix.ext }}
      - name: "Moving Resources Directory to Distribution"
        run: |
          cp -r noise-suppression-lv2/resources/* dist/
          mv dist/noise_suppression_lv2.${{ matrix.ext }} dist/noise_suppression.lv2/
        if: matrix.package == 'lv2'
      - uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.os_name }}-${{ matrix.arch }}-${{ matrix.package }}
          path: dist/*
